spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/reitera_db
    username: reitera_admin
    password: reitera_password

app:
  cors:
    allowed-origins: http://localhost:4200

api:
  security:
    jwt:
      secret-key: "test-secret-key-for-tests-only-not-used-in-production"
      expiration-time: 900000
    refresh-token:
      expiration-days: 7
    resend:
      api-key: "test-dummy-key"
